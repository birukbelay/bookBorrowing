import 'package:flutter/material.dart';

void main(){
  runApp(
    new MaterialApp(
      home: new Center(
        child: ListView(
          children:<Widget>[
            Text('one'),
            Text('one'),
            Text('one'),
            Text('one'),
          ],
        ),
      ),
    )
  );
}